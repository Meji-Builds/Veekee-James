# Password for signing in at /dashboard. Pick something long and random.
DASHBOARD_PASSWORD=change-me

# Upstash Redis (Vercel Marketplace > Upstash > Redis), REST API credentials.
# Without these, the dashboard runs but changes won't persist.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
