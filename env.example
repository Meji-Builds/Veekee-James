# Password for signing in at /dashboard. Pick something long and random.
DASHBOARD_PASSWORD=change-me

# Redis connection string. On Vercel: Storage > Create Database > Redis, then
# Connect it to this project, it injects REDIS_URL automatically.
# Without this, the dashboard runs but changes won't persist.
REDIS_URL=
